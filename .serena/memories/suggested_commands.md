# Suggested Commands

## Development (run from ad-website-2/)
- `npm run dev` — Start Vite dev server
- `npm run build` — Production build (outputs to dist/)
- `npm run preview` — Preview production build locally

## System (Windows/WSL)
- Project path: `\\wsl$\Ubuntu\home\ben\a1_claude_code_projects\Skills-hub-ad-website-2`
- Git: `git` (standard commands)
- Shell: WSL Ubuntu (bash-compatible)

## Deployment
- Vercel (configured at root level via root package.json)
